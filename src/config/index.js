export const API_PROTOCOL = 'http';
export const API_HOST = 'localhost';
export const API_PORT = 3000;
export const API_VERSION = 'v1';

export const API_BASE_URL = `${API_PROTOCOL}://${API_HOST}:${API_PORT}/api/${API_VERSION}`;
